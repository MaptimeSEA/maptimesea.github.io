source "https://rubygems.org"

# This gem automatically includes the correct Jekyll version
# and all allowed plugins (like jekyll-seo-tag, jekyll-sitemap, etc.)
gem "github-pages", group: :jekyll_plugins
